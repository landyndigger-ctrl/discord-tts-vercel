export const metadata = {
  title: "Discord TTS",
  description: "Chromebook-friendly Discord TTS app",
  manifest: "/manifest.json",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
